import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'todo.ionic.angular',
  appName: 'todo-ionic-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
